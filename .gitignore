# ── secrets ──────────────────────────────────
.env

# ── data / runtime state ─────────────────────
data/
trades/
scheduled_events.json

# ── polymarket ───────────────────────────────
polymarket/market_relevance.json
polymarket/polymarket_markets.json

# ── bloomberg ────────────────────────────────
bloomberg/monitor_state.json
bloomberg/bloomberg_feed.json
bloomberg/bloomberg_session.json
bloomberg/chrome_profile/
bloomberg/liveblog_history.json

# ── old paths (keep for safety) ──────────────
monsieur_market_state.json
monsieur_market.log
market_relevance.json
whale_wallets.json

# ── python ───────────────────────────────────
__pycache__/
*.pyc
*.pyo
venv/